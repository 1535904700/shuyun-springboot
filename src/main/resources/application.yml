server:
  port: 8080
  servlet:
    context-path: /
spring:
  datasource:
    username: root
    password: rootpan
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test?useUnicode=true&characterEncoding=utf-8
logging:
  level:
    com.shuyun.springboot: error

